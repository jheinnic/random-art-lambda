import { IAdapterFactory } from "./IAdapterFactory.js"
import { IExtensionAdapterClass } from "./IExtensionAdapterClass._st"

// export interface DiscoverableAdapter<A extends DiscoverableAdapter<A>> { }
// extends DiscoverableAdapter<ExtensionAdapter<K>> {
// export type ExtensionAdapter<K extends string>

// export type SameAdapterAltKey<T extends ExtensionAdapter<any>, K extends string> =
// T extends DiscoverableAdapter<infer I> ? I<K> : never;

// export interface IAdapterFactoryFactory<in ExtensionPoint extends string> {
export interface IAdapterFactoryFactory {
   prepare: <
      ExtensionPoint extends string,
      ExtensionApi extends {},
      AdapterType extends IExtensionAdapterClass<ExtensionPoint, ExtensionApi>,
   >(
      extensionPoint: ExtensionPoint,
      AdapterType: AdapterType,
   ) => IAdapterFactory<ExtensionPoint, ExtensionApi, AdapterType>
}
