import { computePixel, newNewPicture, phraseToSeed } from "../painting/components/genjs6.js"

console.dir({ newNewPicture, computePixel, phraseToSeed }, { depth: Infinity })
console.dir(newNewPicture([3, 5, 7, 8], [28, 28, 76, 271]), { depth: Infinity })
